{
  "name": "mysql-docker",
  "private": true,
  "engines": {
    "node": "14.x"
  },
  "bin": "scripts/index.js",
  "scripts": {
    "start": "yarn build && node scripts/index.js",
    "build": "tsc --project tsconfig.build.json",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "commander": "^8.1.0",
    "execa": "^5.1.1",
    "npm-run-all": "^4.1.5",
    "typed-config": "workspace:packages/typed-config",
    "typescript": "^4.3.5"
  }
}
