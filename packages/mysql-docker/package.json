{
  "name": "mysql-docker",
  "private": true,
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "start": "yarn build && node scripts/index.js",
    "build": "yarn codegen && tsc",
    "prepublish": "yarn build",
    "codegen": "dotenv-generator generate"
  },
  "dependencies": {
    "commander": "^7.2.0",
    "execa": "^5.1.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.3.2"
  }
}
